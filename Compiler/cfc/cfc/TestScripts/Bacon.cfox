class Vector2
{
	var x = 0;
	var y = 0;

	init(X, Y)
	{
		this.x = X;
		this.y = Y;
	}

	func toString()
	{
		return "(x: " + this.x + ", y: " + this.y + ")";
	}
}

let vec = Vector2(1, 2);

print vec.toString();

var b = Bacon();

b.describe();
